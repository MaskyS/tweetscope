# Frontend (multi-user hosted) Vercel env
# Project: web-app

# Must point to hosted API project base URL including /api
VITE_API_URL=https://api.example.com/api

# Optional if deploying under a sub-path. Usually leave empty on Vercel custom domain root.
BASE_NAME=
