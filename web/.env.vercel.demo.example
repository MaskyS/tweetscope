# Frontend (read-only demo) Vercel env
# Project: web-demo

# Must point to demo API project base URL including /api
VITE_API_URL=https://api-demo.example.com/api

# Optional if deploying under a sub-path. Usually leave empty on Vercel custom domain root.
BASE_NAME=
